# Changelog

## 1.0.1

- Fixed pub.dev scoring issues: description length, repo URL, example naming, doc comments.
- Updated topic tags to max 5.
- Added generated `duotoneOutline` and `duotoneFilled` icon weights.

## 1.0.0

- Initial release of the official `vezham_icons` package for [vezham.com](https://vezham.com)
- Generated outline and filled weights via `Vezham.outline.*` and `Vezham.filled.*`
- `reiconSvg()` helper to build complete SVG strings
- Pure Dart — zero dependencies, works on all platforms
